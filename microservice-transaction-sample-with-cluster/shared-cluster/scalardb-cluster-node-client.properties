# For running Schema Loader from the host (not from inside a container).
# The service properties point at `indirect:scalardb-cluster-node`, which only resolves on the
# compose network; from the host the node is reached through the published port.
scalar.db.transaction_manager=cluster
scalar.db.contact_points=indirect:localhost
scalar.db.contact_port=60053
